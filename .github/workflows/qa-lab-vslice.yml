name: Laboratorio QA V-Slice

on:
  workflow_dispatch:
  push:
    paths:
      - 'mods/**'
      - 'dist/**'
      - 'Mods .zip terminados/**'
      - 'sync-candidates/**'
      - 'tools/run_qa_lab.py'
      - '.github/workflows/qa-lab-vslice.yml'

permissions:
  contents: read

concurrency:
  group: qa-lab-vslice-${{ github.ref }}
  cancel-in-progress: true

jobs:
  qa-lab:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Configurar Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Instalar dependencias de sistema
        run: |
          sudo apt-get update
          sudo apt-get install -y ffmpeg

      - name: Instalar dependencias de QA
        run: |
          python -m pip install --upgrade pip Pillow
          ffmpeg -version
          ffprobe -version

      - name: Ejecutar 20 rondas QA históricas
        run: python tools/run_qa_lab.py . --rounds 20 --clean --previews --workers 4

      - name: Ejecutar QA paralelo V2.2.2 (20 rondas × 20 mods)
        run: |
          python tools/validate_v222_runtime_contract.py .
          python tools/run_qa_v222_20x20.py

      - name: Publicar artifacts de QA
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: qa-lab-vslice-${{ github.run_id }}
          path: |
            qa-lab/artifacts/**
            qa-lab/baseline/**
            qa-lab/final/**
            qa-lab/previews/**
            qa-lab/rebuild-v222/**
          if-no-files-found: error
          retention-days: 30

      - name: Declarar política segura
        run: |
          echo 'Este workflow no modifica charts, audio, assets ni ZIPs.'
          echo 'Las correcciones deben pasar gates, rollback y revalidación en una rama de entrega separada.'
